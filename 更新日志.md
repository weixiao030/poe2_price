# 更新日志

- 修复 POE1 国服 WeGame 仍可沿用国际服“汉化补丁”语言模式的问题。识别到国服后，界面会自动切回并锁定“自动识别”，固定写入简体中文资源表，同时隐藏“一键汉化 POE1 国际服”；脚本底层也会忽略历史保存或命令行传入的国际服语言模式，避免提取不存在的繁体中文 DAT。

本文件保留完整更新记录，README 只展示最近三条。

---

### 26/8/29 更新（v0.6.1）

- 修复自动识别模式下“价格赛季”控件被隐藏的问题。赛季选择现在固定显示在“游戏版本”右侧；自动识别客户端下拉框切换 POE1/POE2 时，会重新读取对应 realm 的赛季目录，默认选择最新当前软核赛季。
- 保持 POE1/POE2 赛季标识、价格源和缓存严格隔离，切换客户端不会复用另一代游戏的赛季数据。

---

### 26/8/28 更新（v0.6.0）

- GUI 新增“价格赛季”下拉框。启动或切换 POE1/POE2 客户端时，实时访问 poe2scout 的 POE2 `/poe2/Leagues` 或 POE1 `/pc/Leagues` 赛季目录，过滤 Hardcore，默认将 `IsCurrent=true` 的最新软核赛季置顶；点击“刷新”即可主动发现刚发布的新赛季，无需修改代码或手动更新写死名称。
- 选中的赛季同时保存 poe2scout `ShortName` 和 poe.ninja `Value`，更新脚本将两个标识显式传入构建器；价格摘要、缓存键和 POE1 缓存元数据均包含赛季，避免不同赛季复用补丁或价格。
- 历史赛季禁用没有赛季参数的 PoEDB/国服当前赛季源；POE1/POE2 国服只允许当前赛季并在界面和脚本中明确拒绝历史选择，防止把当前数据混入历史赛季。
- 新增赛季目录、POE1/POE2 realm 隔离和当前赛季排序回归测试，源脚本与发布 payload 同步，版本更新为 `v0.6.0`。

---

### 26/8/23 更新（v0.5.9）

- 国服 POE1 圣甲虫恢复国际服回填：`poecurrency-cn` 缺价时仍可用 ninja / scout / poedb 补上圣甲虫价。
- 只读数据源审计补上 POE1（poe.ninja、poecurrency version=1、poe2scout PC、poedb），并刷新契约 baseline。
- poe2scout 分类清单纳入构建健康项：新传奇分类不再默默丢弃；ninja 未覆盖的通货分类写入告警。
- 修复部分电脑启动内置 Python 时要求提权（Win32 740）的问题：以 `RunAsInvoker` 调用，并优先使用 `poe_python.exe`。成功构建后不再把 dat size 中的 2740570 误判成提权失败。
- 修复发布包 `payload.zip` 与源脚本换行不一致导致 Windows Release 检查失败的问题。

---

### 26/8/13 更新（v0.5.8）

- 修复 POE1/POE2 传奇装备在易刷中无法精确查价或误选同底材传奇的回归。当前易刷的 POE1 与 POE2 解析器使用不同标题清洗顺序，不能安全共用一种价格标签。

- 默认值按游戏代数拆分：POE1 使用 `传奇名[<<价格>>]`，其易刷解析器会删除行尾方括号；POE2 使用 `[价格|传奇名]`，其易刷解析器会保留竖线后的名称。PoE Overlay II 对两种格式都能得到纯传奇名。

- 回归测试分别复刻已安装易刷的 POE1/POE2 实际运行模块，并锁定两个交叉失败：POE1 会把 `[0.25D|冈姆的壮志]` 整行删空，POE2 会把 `神圣殿堂[<<3.00E>>]` 清成 `神圣殿堂<>`。后者会丢失精确名称并按“荣耀战铠”等底材候选误选“背信弃义”等其它传奇。

- POE1 国际服一键汉化不再用 `Start-Process -Wait` 阻塞 WinForms 消息循环。汉化脚本改为隐藏启动并重定向输出，GUI 每 200 毫秒轮询、刷新已运行秒数和最新阶段；运行期间禁用重复操作和关闭按钮，失败时显示子进程输出，解决下载时间较长时看起来“没有反应、没有进度”的问题。

### 26/8/12 更新（v0.5.7）

- 永久修复 POE2 国际服官方 GGPK 与 Steam/Epic Bundles2 共用 `国际服还原补丁.zip` 的覆盖问题。逻辑包与物理包现在均按 `POE2 + InstallKind + 语言` 命名，工作输出再按游戏绝对路径 SHA256 前 16 位隔离；权威副本只保存在对应游戏根目录的 `.poe2-price-patch`。共享国服/国际服包保留为发行种子和旧版迁移输入，但新流程永远不会覆盖它们。

- POE2 逻辑还原包新增 v2 manifest，绑定游戏代数、客户端类型、GGPK/Bundles2 模式、配置语言、BaseItemTypes/Words/EndgameMaps 路径、BaseItemTypes 结构签名及每个条目的长度和 SHA256。还原前同时检查作用域、允许路径、重复条目、完整性、当前游戏结构与标记零残留，错误客户端的包即使文件名被改过也不能写入。

- POE1 原有文件名隔离继续保留，逻辑 manifest 从 v1 升级到 v2，并补齐模式、语言、结构签名与条目完整性强制校验。旧物理包仍可在 manifest 与当前客户端完全匹配时迁移，避免破坏已有用户备份。

- POE1/POE2 更新器和还原器都新增“无基线自愈”：当前游戏已经含本工具补丁时，在临时目录以 `patch-scope=none` 只清除能够确认归属本工具的 BaseItemTypes、Words 与岛屿提示，验证结构不变且残留标记为零后才生成专属干净基线。POE1 清理模式跳过赛季发现和全部价格网络请求。只有 DAT 损坏、结构无法解析、出现未知修改或平台并发写入时才安全拒绝并提示校验/修复。

- 重复更新不会刷新专属基线：`干净 → 更新 1 → 更新 2 → 更新 3 → 还原` 始终回到第一次写入前的干净数据；写入失败仍优先使用真实物理包回滚。新增 POE2 跨客户端命名、游戏目录隔离、manifest 防篡改、POE1 v2 manifest 和离线清理回归测试，并用当前已打补丁且缺少正确基线的国际服 GGPK 完成真实自愈验证。

### 26/8/12 更新（v0.5.6）

- 此版本曾尝试修复传奇查价兼容性，将默认标签从 `[价格|传奇名]` 改为 `传奇名[<<价格>>]`。

- 后续检查易刷实际运行模块确认，POE1 与 POE2 使用不同清洗器：后缀格式在 POE1 正常，但在 POE2 会残留 `<>`；竖线格式在 POE2 正常，但当前 POE1 会把整行方括号删除。

- 后缀标记继续作为 POE1 默认值；更新、还原和 Words 状态检查同时识别后缀、竖线、Overlay 专用和独立行标记。

- v0.5.8 已将回归测试替换为两套易刷真实清洗顺序，并将 POE1/POE2 默认参数拆开。

### 26/8/11 更新（v0.5.5）

- 修复 POE1/POE2 国服与国际服 Bundles2 客户端在官方更新或连续运行物价更新后，旧真实还原包仍拿“首次安装前”的文件数量/SHA256 指纹校验当前已安装状态，导致本工具自己上次写入的 `LibGGPK3` 被误报为并发变化、尚未调用 `PatchBundle3` 就退出的问题。成功写入并读回校验后现在会在游戏目录持久记录与真实还原包绑定的现场指纹；后续更新可安全复用，旧版本没有该记录或检测到第三方/平台更新时则在离线沙盒中刷新干净还原基线。最终写入前仍使用本次运行刚捕获的指纹复核，真正的平台更新并发不会被放行；GGPK 客户端不经过该流程，不受此问题影响。

- 并发状态提示改为“与本次写入准备时不同”，并明确要求等待游戏平台更新完成、完全关闭游戏与启动器后重试，避免把安全中止误解为价格数据为空。

- 修复 POE1 逻辑还原包中的校验 manifest 被 `PatchBundle3` 当成游戏资源写入而引发 `FileNotFoundException` 的问题；写入前会生成只包含 BaseItemTypes/Words 的临时载荷，原 manifest 继续用于完整性与版本校验。同时统一 Windows 8.3 短路径与长路径后再生成真实还原文件清单，避免 `%TEMP%` 为 `CATMAO~1` 等短路径时误报 `_.index.bin` 不存在。

- 使用本机当前 POE1 国服 WeGame、POE1 国际服 Steam 与 POE2 国服 WeGame 客户端执行真实隔离验证：复制当前 Bundles2 后使用真实 `PatchBundle3 v2.7.5` 连续写入两次当前实时构建补丁并逐次读回 BaseItemTypes/Words；POE1 还额外重建并验证了干净物理还原包。三套客户端全部通过，真实游戏目录的 Bundles2 完整指纹前后不变。

### 26/8/10 更新（v0.5.4）

- 修复 POE1 Steam Bundles2 更新失败：旧流程分别提取本地化 DAT 和英文 DAT，第二次加载 `Bundles2\_.index.bin` 时可能遇到 `IOException` 文件占用，进而找不到英文 `BaseItemTypes`。现在四个候选表一次批量提取，英文 `BaseItemTypes` 作为实时构建必需项校验，并对中英文文件占用提示和 sharing violation 做有限重试。每次都以当前游戏 DAT 为底板重建，游戏更新后不复用不兼容赛季资源。

- 统一 GUI 的“一键汉化POE1国际服”加入多源自动切换：`ghfast.top`、`gh-proxy.com`、`gh.ddlc.top`、`ghproxy.it`、`github.boki.moe`、`ghproxy.net`、`gh.jasonzeng.dev`、`gh.monlor.com`，最后回退 GitHub 官方源；全部下载都必须匹配最新正式 Release 的 SHA256，API 可用时还会严格匹配文件大小，不使用旧缓存。`bdnb.cn` 因交互密码/匿名限速未接入，Gitee 未找到可验证的具体镜像仓库。

- 汉化工具完成后自动尝试设置 `production_Config.ini` 为 `language=fr`，用户进入游戏请选择第二个（法文）国旗。汉化只支持 POE1 国际服，国服会明确拒绝；文档、payload、测试和统一启动器版本同步为 0.5.4。

### 26/8/4 更新（v0.5.3）

- 修复国际服官方 GGPK 安装在 `D:\poe2` 等非默认目录时，首次运行自动识别不到的问题。根因是客户端把实际路径写在 `HKCU\Software\GrindingGearGames\Path of Exile 2\InstallLocation`，此前程序只读取卸载项、平台游戏库和常见目录，没有读取 GGG 自有注册表项。

- POE1 与 POE2 现在都会检查当前用户及本机范围的 GGG 官服注册表，并读取 `InstallLocation`、`InstallPath`、`Path` 或 `GamePath`。注册表候选仍必须通过真实目录和 `Content.ggpk` / `Bundles2\_.index.bin` 校验，失效记录不会进入 GUI。

- 新增 POE1/POE2 官服注册表发现回归测试，同步发布 payload、统一 `物价补丁.exe`、README 与使用文档到 v0.5.3。

### 26/8/4 更新（v0.5.2）

- 重新审计 `poecurrency.top`、PoEDB Economy、poe.ninja POE1 与 poe2scout 的实时页面和接口。确认此前 POE1 国际服主要只有 poe.ninja，国服只有 poecurrency.top 主源加 poe.ninja 补缺，并没有沿用 POE2 的完整多源方案。

- POE1 国际服升级为 `poe.ninja → poe2scout → PoEDB` 逐物品补缺；国服升级为 `poecurrency.top → poe.ninja → poe2scout → PoEDB`。后续来源只补缺少的元数据路径，不覆盖前序价格；各来源并行抓取、独立记录错误和降级状态，POE2 构建器与数据源优先级不变。

- 接入 poe2scout 新增的 POE1 `pc` realm：自动发现当前软核赛季，读取 `ReferenceCurrencies` 与 `SnapshotPairs`，使用 `BaseItemTypeId` 直接匹配 DAT 元数据路径，避免只靠市场名称造成漏价或串价。

- 接入 PoEDB POE1 Economy 的 16 个动态分类、约 1021 条国际服数据。价格使用 `/us/Economy`，`/cn/Economy` 只用于简中名称辅助匹配；`/tw/Economy` 是台湾服价格，不混入 Steam 国际服或 WeGame 国服。POE1 按 Chaos/Divine 换算，不复用 POE2 的 Exalted 基准。

- poe.ninja POE1 新增 Runegraft、DjinnCoin、Ducat、EnshroudingCrystal、Astrolabe、Invitation、Vial 等可稳定对应一个游戏物品名的分类；Incubator、Memory 保留空赛季兼容。SkillGem、BaseType、ClusterJewel、ValdoMap、ForbiddenJewel、Wombgift 等价格依赖等级、品质、词缀或具体变体，不能安全压成一个固定 DAT 名称价格，因此明确不接入。

- `summary.json` 新增各来源状态、抓取价格数、匹配数和实际补缺数。2026-08-04 对本机真实 Steam/国服客户端执行“还原、独立读回、更新、独立读回”最终验证：Steam 写入 829 条基础价格和 1158 条传奇词条，其中 poe.ninja 命中 785 条，poe2scout、PoEDB 分别补缺 23、21 条；国服写入 840 条基础价格和 1157 条传奇词条，其中 poecurrency.top 命中 342 条，poe.ninja、poe2scout、PoEDB 分别补缺 451、29、18 条。覆盖数会随实时市场数据变化。

- 修复数据源审计器无法解析 poe.ninja 当前 Astro 模块反引号字符串的问题，并增加多源优先级、精确路径、Chaos/Divine 换算、安全分类和反引号结构回归测试。发布脚本、README、使用文档和统一启动器同步到 v0.5.2。

### 26/8/4 更新（v0.5.1）

- 修复 POE1 Steam 客户端安装第三方汉化补丁后价格不可见的问题：此前只读取 `production_Config.ini`，例如配置为 `language=fr` 时会把价格写入法语 DAT，但汉化补丁实际让游戏显示繁中 DAT。现在会区分原始配置语言与实际写入语言。

- 统一 GUI 新增“POE1 显示语言”，支持自动识别、汉化补丁、简体中文、繁体中文、跟随游戏配置。选择会保存到 `%LOCALAPPDATA%\PoePricePatch\settings.json`，更新与还原显式使用同一语言模式；POE2 界面和脚本行为不变。

- POE1 自动模式下国服固定简中；国际服配置已是中文时直接使用；配置为非中文时读取最新 `LatestClient.txt` / `Client.txt` 的区域记录，若最新有效区域名含中文则按汉化补丁写入繁中表，否则继续跟随配置。`POE1_PATCH_LANGUAGE` 环境变量仅在自动模式下保留兼容。

- POE1 价格缓存、逻辑还原包和 Bundles2 真实还原包改用实际写入语言隔离，避免法语、繁中等目标互相覆盖；还原时仍会查找并严格验证旧版不带语言后缀的真实还原包。

- 新增日志顺序、五种语言模式、设置持久化、GUI 参数转发和还原包命名回归测试；发布 payload、启动器、README、使用文档同步到 v0.5.1。

### 26/8/4 更新（v0.5.0）

- 新增独立 POE1 物价补丁，接入 poe.ninja POE1 经济接口与国服 `poecurrency.top/api/summary?version=1`，使用混沌石 / 神圣石（C/D）显示普通物品和传奇装备价格。

- 更新与还原统一进入 POE 物价补丁 GUI，支持 POE1、POE2、自动识别，以及国际服官方 GGPK、Steam/Epic Bundles2、WeGame Bundles2 三类客户端。

- 发布包统一只保留 `物价补丁.exe`；打开后在 GUI 内选择“更新物价补丁”或“还原物价补丁”，统一按钮为“开始/更新物价补丁”，不再生成两个分开的入口。

- 优化统一 GUI：移除顶部操作切换，把“还原物价补丁”放到底部操作区，与“开始/更新物价补丁”并列；修复发布版启动器与 PowerShell 工作进程重复获取同一游戏锁导致单次启动也提示“正在运行”的问题。

- 修复自动识别把同一个 Bundles2 目录同时列为 POE1 和 POE2 的问题；现在会用 Bundles2 内容探测结果过滤错误版本，国服和 Steam 客户端各只显示一次。

- 修复 POE1 更新从统一 GUI 传参时路径可能错配到 `PatchScope` 的问题，改用显式参数映射，避免把游戏目录误当作更新范围。

- POE1 与 POE2 使用独立的脚本、缓存、输出和还原包；POE2 继续使用原有 E/D 规则，新增 POE1 不会改写 POE2 价格或还原数据。

- BundleExtractor 增加 GGPK 识别和批量提取接口；发布 payload、启动器、文档和回归测试同步到 v0.5.0。

### 26/7/25 更新（v0.4.9.7）

- 修复 [Issue #23](https://github.com/weixiao030/poe2_price/issues/23)：国际服同显示名的多个 BaseItemTypes 元数据别名不再因字典覆盖而漏写价格，国服翻译重名时使用 engname 进行精确消歧。

- 同步源脚本、发布 payload、启动器和测试，并使用最新版官服 Content.ggpk 验证巅峰门票、君王之颅、骷髅马克等同名别名均能生成价格。

### 26/7/25 更新（v0.4.9.6）

- 更新和还原入口统一支持自动识别与手动选择游戏目录。补丁文件夹可放在任意位置，确认后的有效目录会原子保存到当前 Windows 用户的本地设置；目录移动或游戏卸载后，失效记录会被自动忽略。

- 国服发现规则新增 WeGame 注册信息、WeGame 卸载项、环境变量和各固定/移动磁盘的 `WeGameApps\rail_apps` 常见游戏库；名称识别同时支持 `Path of Exile 2`、`流放之路2`、`流放之路：降临` 和 `POE2`。所有候选仍必须实际包含 `Content.ggpk` 或 `Bundles2\_.index.bin`。

- 自动发现多个客户端且没有补丁父目录、环境变量或最近目录等明确选择时，不再按优先级静默取第一个，而是列出候选并要求手动选择，避免误更新或误还原。GUI 状态会显示“国际服官方 GGPK”“国际服 Steam/Epic Bundles2”或“国服 WeGame Bundles2”。

- 修复一键还原仍把补丁文件夹上一级当作游戏目录的问题。独立启动器把脚本解包到 `%TEMP%` 时，还原器现在会先显示目录选择窗口，不再在临时目录中检查 `Content.ggpk` / `Bundles2`；未处理异常改为简洁中文错误，不再输出整段 PowerShell 堆栈。

- 启动器会优先使用补丁父目录或最近确认的游戏目录计算实例锁；更新和还原脚本在最终选择后再获取真实游戏目录级互斥锁，覆盖不同补丁副本同时操作同一客户端的情况。更新失败触发的内部自动回滚会显式复用父流程持有的锁，不会发生自锁。

- 无权限或失效的注册表/文件候选会被静默忽略，不再在自动发现过程中输出无关的 `Access is denied`；新增国服游戏库、多客户端歧义、最近目录、环境变量优先级、还原 GUI 和发布同步回归测试。

- 修复 [Issue #23](https://github.com/weixiao030/poe2_price/issues/23)：国际服价格匹配原先使用单值名称索引，`BaseItemTypes` 中同一个英文/显示名对应多个元数据路径时只保留最后一条，导致巅峰门票、君王之颅、骷髅马克等物品不显示价格。现在会保留并写入全部同名元数据别名。

- 国服价格匹配继续支持中文显示名，同时在翻译重名时使用 `engname` 英文别名消歧，避免把一个物品的价格错误应用到另一个物品。

- 同步源脚本与 `build/payload` 内嵌脚本、测试和发布产物；使用最新版官服 `Content.ggpk` 回归验证，修复后匹配行数由 565 增至 582，目标问题物品均无缺失价格。

### 26/7/11 更新（v0.4.9.5）

- 发布目录新增固定版本和 SHA512 的 `.NET 8.0.28` 离线修复 ZIP。内置运行时缺失或损坏时优先从 `tools/downloads` 原子恢复，不产生网络请求；只有修复 ZIP 也缺失或损坏时才依次使用 Microsoft CDN 与官方源，8.0.27 继续作为版本级后备。

- Python 3.13.14 下载顺序改为华为云、阿里云、南京大学镜像，最后使用 Python 官方源。华为云镜像已实际下载完整文件并核对 SHA256，与官方包一致。

- Python 国内镜像单次超时设为 90 秒、每个来源最多重试两次；官方备用源使用 180 秒超时。单个镜像不可用、超时、返回 HTML 错误页或哈希不一致时，会删除临时文件并自动尝试下一来源。

- 发布构建为 Python 采用国内镜像优先、官方备用的下载链路，同时强制用固定归档生成 .NET 8.0.28 运行时和离线修复副本，避免构建机旧版运行时混入发布包。所有来源共用固定 SHA256/SHA512，下载来源改变不会降低供应链校验强度。

- 新增国内源优先级、官方兜底顺序、固定哈希与发布文档回归检查。

### 26/7/11 更新（v0.4.9.4）

- 更新器新增隔离构建目录。所有价格 DAT 与 ZIP 先在临时目录生成、检查 BaseItemTypes 结构及 ZIP 可读性，成功后再原子发布；构建失败不会动公开输出，更不会进入游戏写入。

- 实时价格采用分层降级：完整数据失败时重试不含 Words 的核心层，再尝试与当前客户端、语言、补丁范围、构建模式和布局兼容的最近成功缓存。缓存降级包只保留已做结构签名检查的 BaseItemTypes，绝不带入旧 Words/EndgameMaps。候选不可用时安全停止本次安装，保留用户当前游戏状态。

- poecurrency 重复记录不再无条件采用最高价格，而是优先选择无错误、非异常、时间新鲜、单位明确且属于当前记录的样本；单位元数据或时间戳批量异常会触发备用数据源。NaN 与 Infinity 不再进入价格计算。

- 本地匹配新增质量门：真实物品表零匹配视为不兼容并停止，低匹配只更新能够可靠对应的行，并由底层构建器实际保留未匹配旧价格。修复同显示名但 metadata 不同的物品被批量串价，以及固定价格模式生成残缺 `D=` 标签的问题。

- 可选 Words 清理失败时复制当前 Words 保持补丁完整且不阻断核心价格层；严格迁移仍会明确失败，防止把无法确认干净的状态当作还原基线。干净 Words 输入现在也会生成完整输出文件，路径推导兼容大小写不同的客户端文件名。

- 岛屿传言 DAT 自动布局增加最低置信度、可读文本数、文本去重和偏移有效性约束；无法唯一确定布局时跳过该可选层，不再猜测并写坏表。

- Bundles2 写入前保存 `_.index.bin`、可选索引和全部 `LibGGPK3` 的长度/SHA256 指纹，写入前再次核对以阻止并发修改；写入后批量提取目标 DAT 读回校验。写入或校验失败会自动调用已验证物理还原包恢复。

- GGPK 写入后同样提取目标 DAT 核对哈希；失败时自动写回逻辑还原 ZIP并验证。还原流程改为物理备份优先，无需先下载 .NET；逻辑还原失败会自动重试一次，仍不一致才报告错误。

- 物理还原在替换后校验顶层可选索引与 `LibGGPK3` 的精确文件集合，检测到外部并发新增或修改会失败并恢复原状态。还原包、价格包、缓存和中间 DAT 全部采用同目录临时文件加原子替换。

- 游戏进程关闭与旧文件清理只在真实安装模式执行；预览、只构建和“不安装”不会终止进程或清理游戏目录。旧安装器进程匹配限制在本项目或游戏目录，避免误杀同名程序。

- 客户端语言配置的默认候选根据国服/国际服路径排序，国际服不再优先读取无关国服配置。游戏文件短暂占用时会有限重试，降低杀毒软件或启动器瞬时扫描导致的失败。

- .NET 8 运行时和嵌入式 Python 下载包增加固定 SHA512/SHA256 校验，官方 Microsoft/Python 源优先，镜像仅作备用；哈希不匹配的下载不会被安装。发布内置 Python 从 3.10.6 升级到仍提供官方 Windows 嵌入包的 3.13.14；新运行时验证成功前保留旧目录。

- BundleExtractor 的路径后备只接受唯一且按完整路径段后缀匹配的候选，多个同名候选会明确失败；启动器修复磁盘根目录路径结尾处理。发布脚本会验证所有还原 DAT 无活动价格/岛屿标记，并在模板段落缺失时拒绝生成不完整文档或遗留半成品。

- 增加零替换、低匹配、异常数据源、并发指纹、自动回滚、物理优先还原、隔离发布、缓存降级与布局置信度回归测试。

### 26/7/11 更新（v0.4.9.3）

- 修复 Bundles2 旧用户升级自锁：旧版本已经写入物价层、但 `真实还原物价补丁.zip` 随补丁文件夹被删除或未曾成功创建时，不再要求用户反复“一键还原”。更新器会清理 BaseItemTypes、Words 与 EndgameMaps 中本工具拥有的活动标记，在临时沙盒副本上运行 PatchBundle3，批量读回校验长度、SHA256 与活动标记，再把沙盒状态保存为带 `semantic-clean-migration` 来源标识的 v2 还原基线。任何一步失败都不会修改真实游戏。

- 真实还原包除当前补丁目录外，还会原子复制到 `<游戏目录>\.poe2-price-patch\真实还原物价补丁.zip`；候选搜索兼容游戏根目录与旧同级补丁文件夹。若找到多个内容不同但均通过指纹、CRC 和 SHA256 校验的包，会明确中止而不是依赖目录枚举顺序。

- 修复 Words 清理后的永久误判：旧价格字符串虽然仍可能作为孤儿字节留在 DAT 尾部，但检测器现在解析行指针，只检查当前有效显示名。BaseItems、Words、EndgameMaps 检测器退出非零或结果无法解析时会报告“无法判断”，不再伪装成“确定已打补丁”。

- 逻辑还原新增 Words 活动标记与最小长度校验；Bundles2 兼容性检查强制重新读取当前索引，不再信任旧输出缓存。旧还原包缺少 EndgameMaps 且当前仍有本工具岛屿提示时，会从当前表生成清理条目补全本次安装包。

- 移除更新安装前的全量 `LibGGPK3` 合并调用。PatchBundle3 只重定向 ZIP 中出现的 FileRecord，未触碰的共存补丁本来就会保留；全量重写会把所有第三方活动内容再次追加，造成增量包持续膨胀和“卡在 PatchBundle3”的假象。写入后的目标长度与 SHA256 校验继续保留。

- 新增旧补丁无物理备份迁移、持久备份双写、探针失败三态语义、Words 孤儿字符串、还原安全与安装顺序回归测试。

---

### 26/7/10 更新（v0.4.9.2）

- 当前软核赛季改为从 poe2scout `/poe2/Leagues` 自动发现，Scout 使用 `ShortName`，poe.ninja 使用 `Value`；同时为 current 的 HC 赛季会被排除。显式 CLI 参数仍优先，失败时使用内置回退值。

- poe.ninja 23 个分类、poe2scout 传奇分类和 Poe2DB Economy 动态分类均增加分类级降级：可选分类请求、空数据或 schema 错误会记为 `partial`并保留健康数据，Currency 核心分类失败仍会进入备用链路。

- Poe2DB 单链接价格格现在只在链接为 divine/exalted、恰好有两个正数、纯文本 slug 与行 key 一致时解析；修复 `liquid-verisium` 及 4 个 sacrifice orb 间歇漏价，双链接旧结构继续支持。

- poecurrency 保留 `engname`、`latest_datetime`、`prev_buy1_datetime`、昨日均价、ratio、`anomaly_count`、`error/error_info`；中文名匹配优先，失败后使用英文别名补齐。显式未知单位跳过并报告，缺失单位保留旧兼容，昨日均价只作最后兜底。

- 新增 `healthy / partial / stale / empty / incompatible / failed / skipped` 统一健康契约，包含 HTTP 状态与 Content-Type、参考通货、数量、分类、时效、无业务值 schema 指纹和漂移报告；摘要保留每次请求耗时、重试次数、响应大小和最终 URL。

- 新增完全只读的四数据源实时审计命令和定时工作流，直接请求实际 API/页面、上传 JSON 报告，权限仅 `contents: read`，不读写游戏、不触发发布。

- 主入口继续保留 CLI、函数名称和输出兼容，共享数据模型、HTTP 客户端、赛季发现和健康契约渐进拆到 `price_sources` 包，内嵌 Python 3.10 仍只依赖标准库。

---

### 26/7/10 更新（v0.4.9.1）

- 修复 [Issue #19](https://github.com/weixiao030/poe2_price/issues/19)：`poe2scout` 的 `SnapshotPairs` 大响应不再使用无上限的整包读取。请求会按 64 KiB 分块读取、限制最大响应体，并受单次墙钟截止时间和单 URL 总预算约束；超时后立即交给现有 `poe.ninja` / Poe2DB Economy 备用链路处理。

- 修复控制台停在“使用 PatchBundle3 写入补丁到 Bundles2”的问题。根因是保留旧 `LibGGPK3` 增量文件时，每个文件都会重新加载并解析一次数百万条索引，且没有进度输出；现在由 BundleExtractor `--extract-list` 一次加载索引并批量提取，同时显示扫描、数量和合并阶段。

- Bundles2 写入前新增游戏进程和索引独占可写检查；写入后会批量读回本次 BaseItemTypes、Words、EndgameMaps 目标，逐项比较长度和 SHA256，防止“命令成功但补丁未落盘”。

- `PatchBundle3` 的 update/restore 调用会显式关闭标准输入，避免错误参数或文件缺失分支等待回车。

- 真实还原包 manifest 升级并记录官方游戏底板指纹；复用和还原前必须与当前客户端匹配，旧版备份在检测到游戏文件晚于备份时会被判为过期，避免官方更新后回滚旧 `_.index.bin`。

- 物理还原改为事务式流程：先完整解压并触发 ZIP/CRC 校验，再保存当前索引和 `LibGGPK3` 回滚副本；替换失败会自动恢复。创建新真实还原包也改为临时文件验证后替换，失败不会删除唯一旧备份。

- BaseItemTypes 兼容性签名新增 `row_count`、`row_size`、metadata 路径哈希和“排除显示名指针后的固定行”哈希，防止游戏仅修改其它字段时把旧整表误判为兼容。

- 修复一次构建没有名称替换时沿用上次 `物价补丁.zip` 的问题；切换为“只更新传奇”“不更新价格”等范围时，不会再把旧通货条目重新装回。

- 一键启动器增加同一补丁目录单实例锁，避免两个更新/还原进程并发重建输出和唯一还原包。

- 发布构建补齐 payload 中的 `BundleExtractor/vcruntime140.dll`，同步源目录一键启动器，CI 增加 Python 测试；脚本、GUI 和可执行文件版本更新为 `v0.4.9.1`。

---

### 26/7/2 更新

- 修复未读取到 POE2 语言配置时国际服错误回退到 English 的问题；现在国际服默认使用繁体中文，国服继续固定使用简体中文。

- GUI 窗口标题显示补丁版本 `v0.4.9`。

---

### 26/7/1 更新

- 修复 `oo2core.dll` 在部分机器上因缺少 VC++ 运行库导致提取器启动失败的问题，发布版会一并携带所需运行库。

- 更新失败提示增强：遇到 `oo2core` / `VCRUNTIME140` / `api-ms-win-crt` 相关错误时，会提示检查工具目录和 Microsoft Visual C++ 2015-2022 x64 运行库。

- GUI 窗口标题显示补丁版本 `v0.4.8`；补丁范围选择正文标题不再重复显示版本号。

- 已同步 `build/payload`、`payload.zip`、`payload.enc` 和发布校验逻辑，确保一键启动器内嵌脚本也是最新版本。

---

### 26/6/29 更新

- 国际服价格源升级为多源合并：会先抓取 `poe2scout` 主数据源，再抓取 `poe.ninja` 备用源；两者都可用时按物品英文名去重合并，优先保留 `poe2scout` 价格，`poe.ninja` 补齐缺失项。

- 国际服兜底链路调整：如果 `poe2scout` 访问失败但 `poe.ninja` 成功，会直接使用 `poe.ninja` 数据；如果 `poe2scout` 和 `poe.ninja` 都失败，才继续尝试 `Poe2DB Economy`。

- `poe.ninja` 备用源已改为抓取全分类数据，不再只抓 Currency 页面；现在包含通货、碎片、符文、精华、灵魂核心、先祖石板，以及传奇武器、防具、饰品、药剂、护符、珠宝、圣所圣物、石板等分类。

- `poe2scout` 传奇装备抓取已改为按分类读取完整分页，避免只拿第一页导致传奇装备数据不全。

- 价格获取过程新增实时进度输出：窗口会显示当前正在抓取哪个数据源、分类完成数、传奇分页进度、请求失败后的重试次数，以及匹配本地物品、写 CSV、生成补丁包等阶段，避免网络慢时看起来像卡死。

- Python 调用方式改为实时透传输出，同时仍会保留完整日志；默认网络请求会自动重试，遇到超时、429、5xx 等临时错误时会显示重试提示。

- 已同步 `build/payload`、`payload.zip`、`payload.enc` 和一键启动器 exe，发布版双击运行也能看到新的进度提示。

---

### 26/6/28 更新

- 修复国服 `poecurrency.top` 价格异常处理：当接口返回 `error=true` 时，不再直接使用可能 OCR 错位的 `latest_buy1` / `latest_sell1` / `prev_buy1`，会优先使用 `buy_avg` / `sell_avg` 计算出的今日均价，均价不可用时才使用 `prev_buy1` 兜底，避免“梦魇拟像”等物品被标成几百 D。

- 修复国服 D 单位小数点错位：自动识别 `2.3 / 225D`、`2.6 / 242D` 这类 100 倍 OCR 错位并折回正常 D 价格；同时给“神圣石”D/E 汇率增加异常防抖，避免汇率小数点识别错误导致所有 D 价格整体放大 10 倍。

- 优化国服最新买卖价差过大的取价策略：`latest_buy1` / `latest_sell1` 差距超过 5 倍时，会参考今日均价选择更可信的一侧，必要时回退到均价，不再简单固定取较低一侧，减少明显低估和高估。

- 已用当前全量 `summary?version=2` 数据做对比验证：532 个源物品中 521 个可出价，49 个 `error=true` 异常条目均可正常提取价格，异常条目中没有残留 50D 以上离谱高价；最终显示价格发生变化 59 个，其中 46 个来自异常条目修正。

- 修复国服更新时备用参考源网络异常会导致整个物价补丁失败的问题：`poecurrency.top` 主价格源成功时，`poe2scout` 或 `Poe2DB Economy` 参考源失败会改为警告并继续生成主价格补丁。

- 更新失败排查信息增强：价格获取或补丁生成阶段的输出会写入 `output\poe2_price_patch_latest\price_patch_build.log`，失败提示会显示日志路径，方便把真实错误一起反馈。

- 生成摘要新增 `cn_reference_status` 和 `cn_reference_warnings`，可用于判断国服参考源是否正常参与了本次价格生成。

---

### 26/6/27 更新

- 新增“岛屿传言提示”可选补丁：更新时勾选后，会在 `EndgameMaps.datc64` 的岛屿传言文本后追加目标地图提示，支持简中、繁中、英文、日文、韩文、俄文、法文、德文、西班牙文、葡萄牙文和泰文路径；前 5 个固定奖励岛屿会显示金币图、经验图、独特基底装备、传奇装备、首领战等类型，部分特殊传言会显示“地图 / Boss”。

- 发布版启动时新增补丁范围选择：可以选择“通货 + 传奇装备”“只打通货补丁”或“只打传奇装备补丁”；命令行也可用 `-PatchScope all|currency|uniques` 指定。

- 岛屿传言提示在发布版弹窗中默认勾选；如本次不需要，可在弹窗里取消勾选。命令行仍可用 `-IslandRumourHints` 或环境变量 `POE2_PATCH_ISLAND_RUMOUR_HINTS=1` 显式开启。

- 还原包和真实 Bundles2 还原逻辑已兼容 `EndgameMaps.datc64`：生成岛屿提示前会尽量使用干净底板，真实还原时会恢复到安装本补丁前的 `LibGGPK3` 状态，避免旧增量文件残留。

- 发布链路修复：已同步 `build/payload`、`payload.zip`、`payload.enc` 和一键启动器，避免源码已有新功能但发布版内嵌脚本缺失。

---

### 26/6/25 更新

- 适配 `poecurrency.top` 新版 `summary?version=2` 数据结构：现在会识别 `currency_unit` 的 `e` / `d` 单位，`d` 单位会按当前“神圣石”实时汇率折算成 E 后再参与 D/E 显示。

- 如果国服接口后续直接提供 E 计价字段（如 `e` / `price_e`），会优先使用接口给出的 E 值，避免重复换算。

- 已检查国际服 poe2scout 接口，当前 `RelativePrice` / `ValueTraded` 等字段结构未变化，国际服价格逻辑保持兼容。

- 发布版稳定性修复：价格脚本已改用 Python 标准库联网，不再依赖 `requests/urllib3/certifi` 等第三方包；内置 .NET 运行时如果缺少 `System.Collections.dll` 等核心文件，会尝试自动修复。

- Bundles2 还原兼容旧包：旧版还原包缺少 `Words.datc64` 时不再直接报“缺少还原包”，会先还原 BaseItemTypes，并提示在游戏文件干净后重新更新以刷新新版还原包。

---

### 26/6/24 更新

- 进行大量的兼容，包括但不止于兼容A大补丁。
- 修复国服通货计算错误导致价格异常离谱的问题
- 当前国服物价策略为如果 poe2scout 国际服参考价 >= 10D，且国服折算价和国际服偏差超过 5x，就用国际服参考价替换。如果国服数据源缺少的数据将使用国际服代替。传奇装备全部数据都是国际服的数据

---

### 26/6/23 更新

- 国服数据源已切换为 `poecurrency.top` 优先：检测到国服 WeGame 时先抓取 `https://poecurrency.top/api/summary?version=2`，没有国服数据的普通物品和传奇装备再使用 poe2scout 国际服价格兜底；国际服仍继续直接使用 poe2scout。

- D/E 换算比例改为实时读取：国服从数据源里的“神圣石”当前价格推导，国际服从 poe2scout 实时价格推导，不写死固定比例。

- 国服取价优先使用最新盘口 `latest_buy1` / `latest_sell1`，缺失时才回退到 `buy_avg` / `sell_avg`；双边价差在 5 倍以内取几何均值，差距过大时会结合今日均价选择更可信的一侧或回退均价，降低过期均价、OCR 异常价和小数点错位影响。

- Bundles2 模式兼容先安装的功能/词缀补丁：更新物价时会保留当前游戏包里的资源底板，只清理并替换本工具写入的物价标记。`_.index.bin` 的更新时间会随写入变化，这是 PatchBundle3 更新索引的正常现象，不代表完整覆盖 `Tiny*.bundle.bin`。

---

### 26/6/20 更新

- 传奇装备无法查价问题已修复。已经兼容易刷/Overlay查价器，如果别的查价器有问题不兼容请留言。

- 新增传奇物品价格标注：会抓取饰品、防具、武器、珠宝、地图等七种分类等中排名靠前的传奇物品价格。

- 当前传奇物品抓取的市场物品的市场最低价，真实物品价值还得根据词条等多方位判断

- 优化价格显示：低于 1E 的物品不再标注；低于 0.1D 的价格改用 E 显示，避免出现 0.0xD 这种不直观的显示。

- 目前英文客户端因为底层问题使用的话会影响到过滤器正常使用,其他语言客户端没影响.
