# 当前版本（v0.8.5）

- POE2 通货更新范围加入碑牌词缀标价，使用自定义碑牌报价 API 和 poe.ninja `PrecursorTablets` 目录。
- 碑牌词缀按现有 D/E 价格逻辑追加到独立描述资源；请求自动重试，失败只跳过碑牌层并继续其它价格流程。
- 繁体中文和简体中文共用独立碑牌模板，负面词缀不追加价格，资源写入失败会回滚碑牌层临时改动。

## v0.8.4

- POE1 / POE2 国服共用本地 OCR 报价校验，继续使用普通汇总接口，无需 Token 或额外历史请求。
- 按同一物品分侧比较当前价、历史均价和上一笔报价，处理双边错价、单边极端值及均价污染，重复字段不重复计票。
- 修复 D 单位绕过校验和神圣石单边异常污染汇率；派生 E 价格需通过一致性校验，国服换算不借用国际汇率。
- 避免正常跨位数涨跌被网站错误标记误判；小数点修正增加依据，持续宽价差不再一律取低价。
- 国际参考只补缺和记录差异，不覆盖已有国服价格；日志增加异常处理原因。
- 增加 OCR、汇率、数值边界及打包文件一致性回归。汇总字段同时受污染仍可能漏判，真实行情突变可能暂时回退历史估值。

## v0.8.3

- 默认自动识别并跟随最新赛季，保留手动固定选择；按 POE1 / POE2、国服 / 国际服分别保存，重启后恢复。
- 修复国服价格接口的版本与赛季参数：当前赛季不传 `season`，历史赛季携带对应参数。
- 国际服目录增加 Scout / Ninja 备用源；目录刷新失败保留选择并使用有效缓存，取消要求玩家自行核实版本的提示。
- 移除写死的旧赛季兜底，补充源不再擅自切换固定赛季；修复历史项误标最新和损坏缓存混用的问题。
- 历史赛季独立缓存，价格不可用时保留已有补丁并报告失败；修复手动选择赛季的保存问题。
- 已有每小时任务保留原固定赛季；选择自动模式并成功手动更新一次后，后续任务跟随最新赛季。
- 增加赛季切换、目录中断、损坏缓存、来源标识及桌面保存回归检查。

## v0.8.2

- 自动更新持久化执行时间，开机、重启和唤醒后补更已到期任务。
- 游戏运行/目录占用每 2 分钟重查；普通失败按 1、5、15 分钟退避，成功后恢复一小时。
- 核对自启路径与 Windows 禁用状态；修复移动免安装目录后关闭自启仍残留旧路径的问题。
- 重复后台启动不弹窗；写盘失败暂停自动调度；损坏确认配置不会执行非更新操作。
- 补充虚拟时钟、真实 Electron 后台恢复、配置异常和安装包自启回归测试。

## v0.8.1

- 保留底部“一键汉化 POE1”入口，位于“还原补丁”左侧，不依赖价格赛季或物价更新。
- 识别或切换客户端时禁用汉化按钮，执行时显示加载状态。
- 增加独立汉化、取消确认、任务互斥和紧凑窗口布局的界面回归检查。

## v0.8.0

- 工作空间顺序统一为“物价补丁 → 运行记录 → 引用设置”。
- 免安装版改为完整文件夹 ZIP，解压后双击 `物价补丁.exe`，无需每次启动时自解包；继续提供 Setup 安装版。
- 优化启动加载：压缩界面资源，后台自启不创建渲染窗口；托盘打开时按需加载。
- 保留物价更新、汉化、岛屿提示、还原、日志、外观、缓存清理和每小时自动更新行为。

详见 [发布说明](docs/release-notes.md)。
