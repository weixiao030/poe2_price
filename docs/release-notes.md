POE 物价补丁 v0.8.7

- 删除游戏中的“底材参考”整行，直接显示 `祭祀碑牌=0.38D` 这样的名称价签。
- 8 类碑牌按底材标识精确匹配 poe.ninja 普通底材报价；缺少普通报价时保留原名，不使用魔法或稀有整件价代替。价格会随市场更新。
- 修复游戏原始繁中裂痕词条多余的 `]`，保留正常文字链接；魔法和稀有碑牌继续逐条标注所有有数据的词缀价格。
- 更新、跳过、关闭和还原同步清理中文与英文表的旧碑牌名称价；底材名称价不依赖词缀模板。
- 碑牌默认随通货更新，两个价格源独立重试、失败跳过；保留岛屿传言默认开启，以及现有通货、传奇、自动更新和还原功能。

升级后请关闭游戏并执行一次物价更新。支持 Windows 10/11 x64，提供 Setup 安装版和完整文件夹免安装 ZIP，均内置运行环境。免安装版须完整解压后运行 `物价补丁.exe`，保留配套 DLL 和 resources 文件夹。发行包未配置代码签名；项目许可及非商业条款不变。
