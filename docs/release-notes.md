POE 物价补丁 v0.8.0

- 工作空间顺序：物价补丁 → 运行记录 → 引用设置。
- 优化界面加载与后台自启；后台自启不创建隐藏渲染窗口、不触发游戏查询，从托盘打开时按需加载。
- 关闭到托盘后释放渲染窗口，保留每小时自动更新、任务执行和运行记录。
- 保留 POE1 / POE2 物价、汉化、岛屿提示、还原、主题与背景、缓存清理、源码与社区链接。

Windows 10/11 x64。推荐 Setup 安装版以获得更快的日常启动；Portable 每次运行需要先解包。两种发行包均内置运行环境。每小时自动更新需先成功手动更新一次，仅手动关闭才停用；应用彻底退出后暂停，下次启动恢复。发行包未配置项目代码签名。项目使用许可及非商业条款不变。
