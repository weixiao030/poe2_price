POE 物价补丁 v0.9.4

修复安装碑牌价格后过滤器报错、无法加载的问题。升级软件后，请先关闭游戏，执行一次物价更新，再启动游戏重新加载过滤器。

- 恢复 8 类碑牌的英文原名，例如将 `[0.37D|Breach Tablet]` 恢复为 `Breach Tablet`，使 `BaseType ==` 精确匹配重新有效。
- 更新时自动清理旧版英文碑牌价签，中文名称价格和碑牌词缀价格继续保留。
- 安装前检查所有英文底材名称，拒绝含价格标记的补丁，避免旧缓存再次带入问题。
- 核对其他补丁层：其余英文底材名称，以及英文传奇名称、地图、词缀表未发现同类修改。无需修改玩家过滤器。

提供完整 Setup 安装包、ZIP 免安装包，以及正式 v0.9.2 → v0.9.4 增量包和完整更新包。免安装 ZIP 须完整解压后运行 `物价补丁.exe`。发行文件被改动或没有匹配增量的旧版本使用完整安装版或免安装版。

软件更新继续优先使用 GitHub 国内主源和备用源，全部失败后使用 ZOS；保留签名、大小和 SHA-256 校验。GitHub 提供全部发行文件，ZOS 提供增量 ZIP、完整更新 ZIP 与相同的签名清单。

已发布版本的发行文件保持不变，避免影响后续增量更新所需的基线。上传凭据与签名私钥不随源码或发行包提供。

旧版 v0.9.3 的 GitHub 发行与在线更新发行存在 7 个文件哈希差异。本次 v0.9.3 在线升级使用完整更新包，兼容两种发行；原样 v0.9.2 使用增量包。完整更新包由软件自动下载和安装，不能当作免安装版直接运行。
