POE 物价补丁 v0.8.0

- 工作空间顺序：物价补丁 → 运行记录 → 引用设置。
- 免安装版使用完整文件夹 ZIP，解压后双击 `物价补丁.exe`，后续启动不再重复解包；Setup 安装版继续提供。
- 优化界面加载与后台自启；后台自启不创建隐藏渲染窗口、不触发游戏查询，从托盘打开时按需加载。
- 关闭到托盘后释放渲染窗口，保留每小时自动更新、任务执行和运行记录。
- 保留 POE1 / POE2 物价、汉化、岛屿提示、还原、主题与背景、缓存清理、源码与社区链接。

Windows 10/11 x64。两种发行包均内置运行环境。免安装版须完整解压到一个文件夹，保留 `物价补丁.exe`、DLL、`resources` 等配套文件，不要直接在压缩包内运行或单独移动 EXE。每小时自动更新需先成功手动更新一次，仅手动关闭才停用；应用彻底退出后暂停，下次启动恢复。发行包未配置项目代码签名。项目使用许可及非商业条款不变。
