POE 物价补丁 v0.8.2

- 每小时物价自动更新保存下一次执行时间，重启、开机自启和休眠恢复后会补更已到期任务，不再重新等待一小时。
- 游戏运行或目录占用时每 2 分钟短检查，不执行下载或游戏写入；普通失败按 1、5、15 分钟退避，成功后恢复一小时间隔。
- 启动时核对自启路径并显示 Windows 禁用状态；修复免安装目录移动后关闭自启仍残留旧路径的问题。
- 重复后台启动不再弹出窗口；写盘失败暂停自动调度并给出提示，避免重复执行。修复磁盘空间后重新开启自动更新即可恢复。
- 损坏的确认配置不会触发还原或汉化；开关保持，等待再次手动更新成功。还原及普通设置变更不会关闭或推迟自动更新，执行期间可以关闭后续调度。
- 保留独立“一键汉化 POE1”、托盘、汉化与物价引擎和游戏运行保护。
- 继续提供 Setup 安装版和完整文件夹免安装 ZIP（`POE-Price-Patch-0.8.2-x64-NoInstall.zip`）。

Windows 10/11 x64。两种发行包均内置运行环境。免安装版须完整解压到一个文件夹，保留 `物价补丁.exe`、DLL、`resources` 等配套文件，不要直接在压缩包内运行或单独移动 EXE。每小时自动更新需先成功手动更新一次，仅手动关闭才停用；应用彻底退出后暂停，下次启动恢复。发行包未配置项目代码签名。项目使用许可及非商业条款不变。
