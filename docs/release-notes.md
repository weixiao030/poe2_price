POE 物价补丁 v0.8.3

- 默认“自动跟随最新赛季”，保留手动固定选择；POE1 / POE2、国服 / 国际服分别记住赛季，重启后恢复。
- 修复国服当前赛季获取失败：价格接口使用数字版本 `1` / `2`，当前赛季省略 `season`，历史赛季携带所选赛季参数。
- 国际服赛季目录增加 Scout / Ninja 备用来源，支持有效缓存；刷新失败保留选择，不再要求玩家自行核实版本。
- 移除写死的旧赛季兜底，修复补充源切错赛季、历史项误标最新和损坏缓存混用的问题。
- 历史赛季补丁缓存独立，获取价格失败时保留已有补丁并报告失败，自动更新会稍后重试。
- 修复手动赛季选择保存失败；保留每小时更新、独立“一键汉化 POE1”、托盘、还原及游戏运行保护。
- 继续提供 Setup 安装版和完整文件夹免安装 ZIP（`POE-Price-Patch-0.8.3-x64-NoInstall.zip`）。

升级后，已有每小时自动更新任务仍保留原先的固定赛季；如需跟随最新赛季，请选择“自动跟随最新赛季”并成功手动更新一次。

Windows 10/11 x64。两种发行包均内置运行环境。免安装版须完整解压到一个文件夹，保留 `物价补丁.exe`、DLL、`resources` 等配套文件，不要直接在压缩包内运行或单独移动 EXE。每小时自动更新需先成功手动更新一次，仅手动关闭才停用；应用彻底退出后暂停，下次启动恢复。发行包未配置项目代码签名。项目使用许可及非商业条款不变。
