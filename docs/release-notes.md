POE 物价补丁 v0.8.1

- 保留底部“一键汉化 POE1”独立入口，位于“还原补丁”左侧；确认后仅运行汉化，不要求选择价格赛季。
- 识别或切换客户端时禁用汉化按钮，避免使用尚未确认的目录；汉化执行时显示加载状态，并禁止重复执行。
- 增加汉化入口、取消确认、国服禁用、POE2 隐藏及普通/紧凑窗口布局的自动检查。
- 汉化与物价引擎不变，继续保留 v0.8.0 的启动优化、托盘、每小时自动更新和还原行为。
- 继续提供 Setup 安装版和完整文件夹免安装 ZIP（`POE-Price-Patch-0.8.1-x64-NoInstall.zip`）。

Windows 10/11 x64。两种发行包均内置运行环境。免安装版须完整解压到一个文件夹，保留 `物价补丁.exe`、DLL、`resources` 等配套文件，不要直接在压缩包内运行或单独移动 EXE。每小时自动更新需先成功手动更新一次，仅手动关闭才停用；应用彻底退出后暂停，下次启动恢复。发行包未配置项目代码签名。项目使用许可及非商业条款不变。
