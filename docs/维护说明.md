# 维护说明

当前唯一入口位于 `desktop`。按根 README 构建和测试；所有发行核心文件由 `docs/core-provenance.json` 追踪，变更原引擎时应同时更新来源说明并运行对应回归。

发布：保持 `desktop/package.json` 与 lockfile 版本一致，先通过 main 的 Build Desktop Release，再推送对应 v 标签。标签构建通过后上传安装版、免安装版及 SHA256SUMS.txt。
