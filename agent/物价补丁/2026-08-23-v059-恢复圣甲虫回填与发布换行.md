# v0.5.9 恢复国服圣甲虫回填并修复发布包换行

## 问题

- 用户要求撤回方案1：国服 POE1 圣甲虫继续允许国际服回填。
- `0f92e6a` 的 Windows Release 在 `Verify committed release assets` 失败：`payload.zip` 里的 `poe2_patch_common.ps1` 是 CRLF，仓库按 `.gitattributes eol=lf` 检出后是 LF。
- 版本号仍停在 v0.5.8。

## 修复

- 删除 `is_cn_isolated_market_item` / `filter_cn_isolated_prices`，国服缺价时圣甲虫重新走 ninja / scout / poedb 回填。
- 保留 POE1 审计、契约 baseline、scout 分类健康检查、Python `RunAsInvoker` / `poe_python.exe`。
- 源脚本和 `build/payload` 统一写成 LF 后再打 `payload.zip` / `payload.enc`。
- 版本升到 v0.5.9，同步 README、更新日志、使用文档和启动器。

## 回归验证

- `tests/test_release_payload_sync.py` 10 passed。
- `test_poe1_bundles2_*` 仍因本机缺少 `Get-FileHash` 失败，HEAD 原版同样失败。
